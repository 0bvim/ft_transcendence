{
  "name": "game-service",
  "version": "2.0.0",
  "description": "Game service API for tournament management and match data",
  "scripts": {
    "build": "tsc --project tsconfig.server.json",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["ft_transcendence", "tournament", "api", "service"],
  "author": "ft-transcendence",
  "license": "MIT",
  "dependencies": {
    "fastify": "^5.4.0",
    "dotenv": "^16.5.0",
    "zod": "^3.22.4",
    "@ft-transcendence/observability": "file:../observability"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "typescript": "^5.5.4"
  }
}
